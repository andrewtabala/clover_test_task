import styled from 'styled-components'

const SearchContainer = styled.div`
    
    `

const SearchTitle = styled.h4`
        margin: 0;
        margin-bottom: 12px
    `

const SearchInput = styled.input`
    
    `

export { SearchContainer, SearchTitle, SearchInput }